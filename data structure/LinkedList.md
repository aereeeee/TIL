# 연결 리스트

```javascript
function LinkedList() {
    const Node = (elem) => {
        thils.elem=elem;
        this.next=null;
    };

    const length = 0;
    const head = null;

    this.append = () => {}
    this.insert
    this.removeAt
    this.remove
    this.indexOf
    this.isEmpty
    this.size
    this.toString
    this.print
}

```